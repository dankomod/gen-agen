export default {
  // Sets new data from the clients forms
  setClientNewData(state, clientNewData) {
    state.clientNewData = clientNewData;
  },
  // Sets the currently selected client
  setSelectedClient(state, selectedClient) {
    state.selectedClient = selectedClient;
  },
};
